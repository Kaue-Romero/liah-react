services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5189:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: bun run dev

  e2e:
    build:
      context: .
      dockerfile: Dockerfile.e2e
    profiles:
      - test
    environment:
      CI: "true"
    command: bun run test:e2e
